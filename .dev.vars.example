# Local development variables for `npm start` / `npm run dev:worker` (wrangler dev).
# This file is copied to .dev.vars automatically on first `npm start`.
# LAN_ORIGIN is filled in automatically by scripts/prepare-local.mjs.
