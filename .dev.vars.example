# Local development secrets for `npm start` / `npm run dev:worker` (wrangler dev).
# This file is copied to .dev.vars automatically on first `npm start`.
# Edit .dev.vars to change your local admin login password. Never commit .dev.vars.
ADMIN_PASSWORD="Suncoast#1234"
